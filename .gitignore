# Intake raw payloads are not ignored — contributors may commit files here in PRs.
# Local users who do not want to commit raw files can add a personal
# .git/info/exclude entry: intake/raw/**

# Python bytecode
__pycache__/
*.pyc
